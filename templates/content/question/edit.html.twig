{% extends 'base.html.twig' %}

{% block title %}Edit Question{% endblock %}

{% block body %}
    {% include 'content/question/_breadcrumb.html.twig' with {entry: 'edit'} %}
    <div class="container my-3">
        <h1>Edit Question</h1>
        <div class="card">
            <div class="card-body">
                {{ include('content/question/_form.html.twig', {'button_label': 'Update'}) }}
            </div>
        </div>

        <div class="card my-3">
            <div class="card-body">
                <a href="{{ path('question_index') }}">back to list</a>
            </div>
        </div>


{#        {{ include('content/question/_delete_form.html.twig') }}#}
    </div>
{% endblock %}
